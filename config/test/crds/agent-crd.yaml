# Minimal stub CRD for assisted-service Agent.
# Covers only the fields this project reads via unstructured.
# Do NOT use in production — install the real CRD from MCE.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: agents.agent-install.openshift.io
  annotations:
    inventory.example.io/stub: "true"
spec:
  group: agent-install.openshift.io
  versions:
    - name: v1beta1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
  scope: Namespaced
  names:
    plural: agents
    singular: agent
    kind: Agent
    listKind: AgentList
