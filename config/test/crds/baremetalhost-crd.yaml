# Minimal stub CRD for Metal3 BareMetalHost.
# Covers only the fields this project reads via unstructured.
# Do NOT use in production — install the real CRD from Metal3/MCE.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: baremetalhosts.metal3.io
  annotations:
    inventory.example.io/stub: "true"
spec:
  group: metal3.io
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
  scope: Namespaced
  names:
    plural: baremetalhosts
    singular: baremetalhost
    kind: BareMetalHost
    listKind: BareMetalHostList
    shortNames:
      - bmh
